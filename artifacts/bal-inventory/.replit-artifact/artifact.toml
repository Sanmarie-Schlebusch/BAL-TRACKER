kind = "web"
previewPath = "/"
title = "BAL Market Inventory Tracker"
version = "1.0.0"
id = "artifacts/bal-inventory"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/bal-inventory run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bal-inventory", "run", "build" ]
publicDir = "artifacts/bal-inventory/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
